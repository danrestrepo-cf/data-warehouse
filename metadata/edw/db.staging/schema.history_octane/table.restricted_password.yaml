name: restricted_password
primary_source_table: staging.staging_octane.restricted_password
primary_key:
- rpw_pid
- rpw_version
columns:
  rpw_pid:
    data_type: BIGINT
    source:
      field: primary_source_table.columns.rpw_pid
  rpw_version:
    data_type: INTEGER
    source:
      field: primary_source_table.columns.rpw_version
  rpw_hashed_password:
    data_type: VARCHAR(64)
    source:
      field: primary_source_table.columns.rpw_hashed_password
  data_source_updated_datetime:
    data_type: TIMESTAMPTZ
  data_source_deleted_flag:
    data_type: BOOLEAN
