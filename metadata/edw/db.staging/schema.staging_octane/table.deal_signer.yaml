name: deal_signer
primary_key:
- dsi_pid
foreign_keys:
  fk_deal_signer_1:
    columns:
    - dsi_deal_pid
    references:
      columns:
      - d_pid
      schema: staging_octane
      table: deal
columns:
  dsi_deal_pid:
    data_type: BIGINT
  dsi_email:
    data_type: VARCHAR(256)
  dsi_first_name:
    data_type: VARCHAR(32)
  dsi_last_name:
    data_type: VARCHAR(32)
  dsi_middle_name:
    data_type: VARCHAR(32)
  dsi_name_suffix:
    data_type: VARCHAR(32)
  dsi_pid:
    data_type: BIGINT
  dsi_signer_id:
    data_type: VARCHAR(256)
  dsi_version:
    data_type: INTEGER
