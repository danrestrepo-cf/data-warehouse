name: product_add_on
primary_key:
- pao_pid
foreign_keys:
  fk_product_loan_add_on_1:
    columns:
    - pao_product_pid
    references:
      columns:
      - p_pid
      schema: staging_octane
      table: product
columns:
  pao_pid:
    data_type: BIGINT
  pao_version:
    data_type: INTEGER
  pao_product_pid:
    data_type: BIGINT
  pao_from_date:
    data_type: DATE
