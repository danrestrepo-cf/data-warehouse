name: smart_doc_note_monitor
primary_key:
- sdnm_pid
foreign_keys:
  fk_smart_doc_note_monitor_1:
    columns:
    - sdnm_smart_doc_note_pid
    references:
      columns:
      - sdn_pid
      schema: staging_octane
      table: smart_doc_note
  fk_smart_doc_note_monitor_2:
    columns:
    - sdnm_lender_user_pid
    references:
      columns:
      - lu_pid
      schema: staging_octane
      table: lender_user
columns:
  sdnm_lender_user_pid:
    data_type: BIGINT
  sdnm_pid:
    data_type: BIGINT
  sdnm_smart_doc_note_pid:
    data_type: BIGINT
  sdnm_version:
    data_type: INTEGER
