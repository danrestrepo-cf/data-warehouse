name: investor_group
primary_key:
- ig_pid
foreign_keys:
  fk_investor_group_1:
    columns:
    - ig_account_pid
    references:
      columns:
      - a_pid
      schema: staging_octane
      table: account
columns:
  ig_account_pid:
    data_type: BIGINT
  ig_lender_group:
    data_type: BOOLEAN
  ig_name:
    data_type: VARCHAR(128)
  ig_pid:
    data_type: BIGINT
  ig_version:
    data_type: INTEGER
