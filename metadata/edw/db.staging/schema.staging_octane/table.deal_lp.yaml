name: deal_lp
primary_key:
- dlp_pid
foreign_keys:
  fk_deal_lp_1:
    columns:
    - dlp_deal_pid
    references:
      columns:
      - d_pid
      schema: staging_octane
      table: deal
columns:
  dlp_pid:
    data_type: BIGINT
  dlp_version:
    data_type: INTEGER
  dlp_deal_pid:
    data_type: BIGINT
  dlp_lp_loan_id:
    data_type: VARCHAR(32)
  dlp_lp_key_id:
    data_type: VARCHAR(32)
